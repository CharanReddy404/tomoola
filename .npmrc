# Why .npmrc: Same pnpm behavior for everyone (devs + CI). Without it, behavior follows
# each machine's global config and can differ. Store stays global by default (no store-dir).

# Install peer dependencies automatically so we don't add them by hand
auto-install-peers=true

# Don't fail install when peer dependency versions don't match (monorepos often have mismatches)
strict-peer-dependencies=false

# Enforce Node/pnpm versions from package.json "engines" (use .nvmrc for nvm/fnm)
engine-strict=true
